<!-- generated-by: groundrules v1.5.0 (adopted) -->
# Intake / Intent — Variolab – A/B Testing

Raw upstream content describing the project intent.

This file is the **raw source**. The structured synthesis (goal, users, constraints, non-goals, acceptance) lives in `docs/VISION.md`.

---

This project was **adopted** into groundrules after it already existed and was published on wordpress.org. There was no separate upstream specification document (client brief, scoping notes); the project intent was authored directly in-repo and lives in three already-tracked files, which served as the source for `docs/VISION.md`:

- **`CLAUDE.md`** — the working contract: stack, structure, security rules, conventions, language policy, workflow. The one-line intent statement at the top: *"Standalone WordPress plugin for whole-page A/B testing: internal DB tracking, persistent-cookie 50/50 split, no third-party dependency."*
- **`README.md`** — developer-facing overview: features, HTML-import/Blank-Canvas pitch, quick start, gotchas, roadmap.
- **`readme.txt`** — the wp.org-facing description, FAQ, privacy, changelog (the published product narrative).

If future upstream inputs appear (a redesign brief, a partner spec, user-research notes), drop them here as dated files (`YYYY-MM-DD-<topic>.md`) and migrate the stable synthesis into `docs/`.
