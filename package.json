{
  "name": "variolab-ab-testing",
  "version": "0.1.0",
  "description": "Variolab – A/B Testing: lightweight A/B testing for pages with internal tracking and a 50/50 cookie split.",
  "private": true,
  "scripts": {
    "env:start": "wp-env start",
    "env:stop": "wp-env stop",
    "env:clean": "wp-env clean all",
    "env:cli": "wp-env run cli wp",
    "env:tests-cli": "wp-env run tests-cli wp"
  },
  "devDependencies": {
    "@wordpress/env": "^11.5.0"
  }
}
